# Dataforest Test Tasks


---

## Загальні вимоги (виконано для обох завдань)

- ✅ Об’єктно-орієнтоване програмування (OOP)
- ✅ Дотримання принципів SOLID
- ✅ DRY (відсутність дублювання коду)
- ✅ KISS (простота та читабельність)
- ✅ Конфігурація через `.env`
- ✅ Модульна, тестована архітектура
- ✅ Використання лише низькорівневих бібліотек для роботи з БД (psycopg2)
- ❌ ORM (SQLAlchemy тощо) **не використовується**

---

## Task 1 — Vendr Product Scraper

### Опис завдання

Збір інформації про продукти з сайту:

👉 https://www.vendr.com/

Скрайпінг виконується **лише для трьох категорій**:
- DevOps
- IT Infrastructure
- Data Analytics and Management

### Зібрані дані

Для кожного продукту зберігається:
- Назва продукту
- Категорія
- Діапазон цін
- Опис

### Технічні вимоги (реалізовано)

- 🔹 HTTP-запити виконуються через `requests`
- 🔹 Парсинг HTML — **виключно `lxml`**
- 🔹 Реалізовано **чергу задач** для обробки продуктів
- 🔹 Використано **multithreading**:
  - кілька потоків-воркерів для скрейпінгу
  - **окремий потік для запису даних у БД**
- 🔹 Усі результати зберігаються в PostgreSQL

### Структура файлів (Task 1)

```
.
├── .env
├── database.py
├── scraper.py
├── main.py
└── requirements.txt
```

## Налаштування середовища

### `.env` (приклад task№1)

```
MAX_THREADS=12
DB_NAME=books_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
```
---

## Task 2 — Books to Scrape (Playwright)

### Опис завдання

Скрайпінг книг з сайту:

👉 https://books.toscrape.com/

### Зібрані дані

Для кожної книги:
- Title
- Category
- Price
- Rating
- Stock availability
- Image URL
- Description


### Технічні вимоги (реалізовано)

- 🔹 **Виключно Playwright** (без requests, aiohttp, BeautifulSoup, lxml тощо)
- 🔹 Multiprocessing (за замовчуванням — 3 процеси)
- 🔹 **Кожен процес ініціалізує власний браузер**
- 🔹 Реалізовано **Process Manager**, який:
  - моніторить subprocess-и
  - перезапускає процес у разі падіння
- 🔹 Опціональна підтримка CDP (Chrome DevTools Protocol)
- 🔹 Усі дані зберігаються в PostgreSQL

### Структура файлів (Task 2)

```
.
├── .env
├── main.py
└── requirements.txt
```


---

## Налаштування середовища

### `.env` (приклад task№2)

```
PROCESS_COUNT=12
DB_NAME=books_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
```

---

## Запуск

```bash
pip install -r requirements.txt
python main.py
```

